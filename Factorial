#include<bits/stdc++.h>
Using namespace std;
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        long long int n,f=1,k=(1e9+7);
        cin>>n;
        for(int i=2;i<=n;i++)
        {
             f=(f*i)%k;
         }
     }
     return 0;
}
